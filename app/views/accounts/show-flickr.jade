extend ../layout
block content
  ul.breadcrumb
    li
      a(href='/admin/accounts') Accounts
      span.divider

    li.active
      = account.name

  //------------------------------------------------------------------------
  .page-header
    h1
      = account.name

  //------------------------------------------------------------------------
  .well
    h4 Login via Flickr now
    a.btn.btn-large.btn-primary(href='/admin/accounts/' + account.id + '/auth/flickr') Authorize

  //------------------------------------------------------------------------
  table.table
    - creds = account.getCredentials()
    dl
      dt Consumer key
      dd
        code= creds.consumerKey
    dl
      dt Consumer secret
      dd
        code= creds.consumerSecret
    dl
      dt Access token
      dd
        code= creds.accessToken
    dl
      dt Access token secret
      dd
        code= creds.accessTokenSecret

    - if (creds.displayName)
      dl
        dt User
        dd
          strong= creds.displayName
          br
          em= creds.username
          br
          em= creds.userId

  //------------------------------------------------------------------------
  .actions
    a.btn.btn-small.btn-danger(href='/admin/accounts/'+account.id, data-method='delete', data-confirm='Really?')
      i.icon-white.icon-trash
      = " Delete account"


