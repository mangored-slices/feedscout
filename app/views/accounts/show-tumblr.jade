extend ../layout
block content
  ul.breadcrumb
    li
      a(href='/admin/accounts') Accounts
      span.divider

    li.active
      = account.name

  //------------------------------------------------------------------------
  .page-header
    h1
      = account.name

  //------------------------------------------------------------------------
  .well
    h4 Login via Tumblr now
    a.btn.btn-large.btn-primary(href='/admin/accounts/' + account.id + '/auth/tumblr') Authorize

  //------------------------------------------------------------------------
  table.table
    - creds = account.getCredentials()
    dl
      dt Consumer key
      dd
        code= creds.consumerKey
      dt Consumer secret
      dd
        code= creds.consumerSecret

  //------------------------------------------------------------------------
  .actions
    a.btn.btn-small.btn-danger(href='/admin/accounts/'+account.id, data-method='delete', data-confirm='Really?')
      i.icon-white.icon-trash
      = " Delete account"


